# 工业热处理预装炉智能体 PRD v3.0

> Heat Treatment Furnace Loading Agent
> 面向热处理车间的智能装炉规划、3D验证与作业指导系统

---

## 0. 文档状态

| 项目     | 内容                                   |
| ------ | ------------------------------------ |
| 文档版本   | PRD v3.1 (更新：2026-06-13)          |
| 当前产品阶段 | 本地 Web 原型 / 单机可演示版本                  |
| 当前重点   | 装炉方案生成、3D 可视化、方案库、JSON 导入导出、PDF 作业指导 |
| 后续方向   | 飞书订单接入、历史方案复用、人工微调、质量/能耗经验模型         |

---

## 1. 产品定位

工业热处理预装炉智能体是一套面向热处理工厂的装炉规划工作台。

系统帮助工厂在实际进炉前，基于炉膛尺寸、工装类型、物料尺寸、重量、材质、工艺、客户与交期信息，自动生成多个装炉候选方案，并通过 3D 数字孪生视图、方案评分、PDF 作业指导书与历史方案库，辅助工艺员、车间主任和装炉工人完成更高效、更稳定、更可复用的装炉决策。

本阶段产品不定位为完整 MES、APS 或 SCADA 系统，也不依赖实时设备采集数据。当前目标是先解决热处理车间中最具体、最高频的问题：

**如何把一批待处理工件合理、安全、可解释地装进合适的炉膛或工装中，并把这次装炉方案保存下来，未来可复用、可追溯、可导出。**

---

## 2. 当前核心问题

热处理工厂在实际装炉中常见问题包括：

1. 装炉高度依赖老师傅经验，方案难以沉淀。
2. 同类订单重复出现，但每次仍需重新人工判断。
3. 不同材质、工艺、硬度、交期的物料容易混在一起，增加质量风险。
4. 炉膛空间利用率、重心稳定性、热场均衡、表面均匀性之间难以平衡。
5. 搁板、料框、料盘、环形工装等不同工装约束难以用普通表格表达。
6. 现场工人缺少清晰的分层装炉指导图。
7. 方案生成后如果不能保存为历史记录，后续无法形成经验库。
8. 客户订单、司机取货、飞书表格、生产排程之间暂时没有打通。

---

## 3. 产品目标

### 3.1 当前阶段目标

当前阶段重点完成一个可演示、可试用、可迭代的装炉工作台：

* 支持手动创建炉膛 / 工装 / 物料。
* 支持 Excel 导入物料数据。
* 支持 JSON 导入导出完整装炉方案。
* 支持多种工装类型与基本物理约束。
* 支持多种装炉策略自动生成候选方案。
* 支持 3D 可视化查看装炉结果。
* 支持搁板、分层、缩略图、爆炸图、施工清单。
* 支持保存方案到本地方案库。
* 支持从方案库加载历史方案。
* 支持恢复物料业务字段，包括客户、产品名、物料编码、材质、工艺、硬度、下单日期、交付日期、备注与 CAD 缩略图。
* 支持 PDF 作业指导书导出。

### 3.2 下一阶段目标

下一阶段重点是把系统从“自动生成结果”升级为“可复用、可修正、可学习”的装炉经验系统：

* 拆分代码模块，提升可维护性。
* 增加历史方案模板复用能力。
* 支持人工微调装炉结果。
* 支持相似订单匹配历史方案。
* 支持飞书多维表格导入订单。
* 支持把装炉结果回写到订单或方案库。
* 支持根据历史质量 / 能耗 / 交期结果做经验评分。

---

## 4. 目标用户

### 4.1 工艺员 / 技术员

关注：

* 同材质、同工艺、同硬度范围是否合理合炉。
* 装炉方案是否满足质量要求。
* 搁板层高、物料间距、热场均衡是否合理。
* 能否导出作业指导书给车间执行。

### 4.2 车间主任 / 生产计划人员

关注：

* 当前订单能否装入现有炉膛。
* 需要几炉完成。
* 哪些物料未能装入。
* 哪个候选方案综合评分更高。
* 是否可以复用历史方案提升效率。

### 4.3 装炉工人

关注：

* 物料放在哪里。
* 第几层放什么。
* 是否需要搁板。
* 每种物料怎么识别。
* 是否有分层图、爆炸图、步骤图。

### 4.4 工厂管理者

关注：

* 装炉效率是否提升。
* 老师傅经验是否可沉淀。
* 质量事故是否减少。
* 是否能逐步接入订单、飞书、MES 或 ERP。

---

## 5. 当前产品范围

### 5.1 已包含范围

当前系统包含以下能力：

1. 炉膛 / 工装配置。
2. 物料批次配置。
3. Excel 导入。
4. JSON 导入导出。
5. 多策略候选方案生成。
6. 3D 装炉结果渲染。
7. 搁板分层显示。
8. 物料筛选与高亮。
9. 方案统计与 AI 摘要。
10. 本地方案库保存与加载。
11. 历史方案恢复。
12. PDF 作业指导书导出。
13. 爆炸图与分层 BOM。
14. 方案按钮状态管理。
15. 基础代码模块拆分。

### 5.2 暂不包含范围

当前阶段暂不包含：

1. 真实 MES 系统对接。
2. 真实 SCADA 实时设备状态采集。
3. 真实 CFD 热流体仿真。
4. 数据库版多用户权限系统。
5. CAD/DXF 精确轮廓识别。
6. 自动排产 APS。
7. 真实能耗闭环校准。
8. 真实质量检测结果回流。
9. 移动端完整适配。
10. 后端云服务账号体系。

这些内容作为后续路线，不应在当前 PRD 中描述为已实现。

---

## 6. 核心业务流程

### 6.1 新建装炉方案流程

1. 用户进入装炉工作台。
2. 添加或选择炉膛 / 工装。
3. 添加或导入待处理物料。
4. 设置装炉规则与策略。
5. 点击生成方案。
6. 系统生成多个候选方案。
7. 用户查看 3D 装炉结果、统计指标、未装入物料。
8. 用户切换候选方案。
9. 用户导出 PDF 或保存到方案库。
10. 后续可从方案库加载该方案。

### 6.2 历史方案加载流程

1. 用户进入方案库。
2. 选择历史方案。
3. 点击加载到工作台。
4. 系统恢复当时的炉膛、工装、物料、规则、搁板和 3D 装炉结果。
5. 用户可继续查看、导出 PDF 或导出 JSON。
6. 后续可基于此方案作为模板继续扩展。

### 6.3 JSON 导入流程

1. 用户打开 JSON 导入弹窗。
2. 粘贴或上传 JSON 文件。
3. 系统校验是否为标准装炉数字孪生记录。
4. 若格式正确，则加载到当前工作台。
5. 系统恢复 3D、物料、搁板、规则与分析面板。
6. 用户可继续保存到方案库或导出 PDF。

---

## 7. 核心数据模型

### 7.1 炉膛 / 工装数据

每个炉膛或工装至少包含：

* 名称
* 宽度
* 高度
* 深度
* 最大承重
* 可用数量
* 工装类型
* 料框显示类型
* 实际安全间距
* 工装参数

当前支持的主要工装类型：

| 工装类型 | 说明                  |
| ---- | ------------------- |
| 标准料框 | 常规高围边料框，可支持搁板分层     |
| 网篮   | 适合小件批量装载            |
| 料盘   | 适合平铺式装载             |
| 环形工装 | 适合圆盘 / 环形 / 井式炉相关场景 |
| 专用夹具 | 当前作为预留类型，后续逐步收敛     |

### 7.2 物料批次数据

每个物料批次至少包含：

* 内部名称
* 产品显示名
* 客户名称
* 物料编码
* 形状
* 长 / 宽 / 高 或直径 / 高度
* 数量
* 总重量
* 单件重量
* 材质
* 工艺
* 硬度要求
* 下单日期
* 交付日期
* 备注
* CAD 图或产品缩略图

### 7.3 装炉结果数据

装炉结果包含：

* 方案 ID
* 方案标题
* 创建时间
* 工艺规则快照
* 炉膛列表
* 每个炉膛内的 packedItems
* 每个工件的位置、尺寸、重量、业务字段
* 搁板信息
* 未装入物料
* 预测结果
* 方案统计信息

当前标准 JSON schema：

```text
heat-treatment-digital-twin-v1
```

---

## 8. 装炉算法与规则

### 8.1 基础算法

当前系统包含以下装炉算法能力：

1. 异构空间填充
   适用于不启用搁板或重心模式时的快速装炉。

2. 搁板分层
   根据当前层最高工件计算搁板高度，并逐层向上摆放。

3. 重心居中
   在 XZ 平面计算重心，Y 轴由炉底或搁板控制，不参与重心评分。

4. 统一嵌套算法
   当启用搁板或重心规则时，系统使用统一算法：
   外层控制 Y 分层，内层控制 XZ 位置评分。

5. 姿态优化
   系统可尝试旋转长方体，使其以更适合装炉的姿态放置。

6. 多炉膛分配
   当一个炉膛无法装完时，系统根据可用炉膛继续尝试后续炉次。

### 8.2 分组规则

当前支持：

* 同材质优先
* 同工艺优先
* 材质聚集率统计
* 工艺聚集率统计
* 按分组独立执行装炉

### 8.3 装炉策略

当前支持四类策略：

| 策略      | 目标                   |
| ------- | -------------------- |
| 重心稳定    | 尽量保持装炉重心居中，同时避免不稳定堆叠 |
| 空间利用率优先 | 尽量提高炉膛空间利用率          |
| 热场均衡    | 尽量避免局部聚集，提高热场均匀性     |
| 表面均匀性优先 | 尽量保持工件表面暴露与气流通道均衡    |

### 8.4 基础约束

当前系统应遵守：

1. 不超过炉膛有效尺寸。
2. 不超过最大承重。
3. 工件之间保留安全间距。
4. 搁板占用实际厚度。
5. 搁板上方才能放置下一层。
6. 环形工装应考虑中心空洞与圆形边界。
7. 不同工艺、不同材质可根据规则分组装炉。
8. 物料业务字段必须随方案保存与加载保持完整。

---

## 9. 3D 可视化需求

### 9.1 基础视图

3D 视图需要显示：

* 炉膛或工装边界
* 物料实体
* 搁板
* 坐标轴
* 标尺
* 网格
* 物料颜色
* 当前炉次

### 9.2 交互能力

当前需要支持：

* 鼠标旋转、缩放、平移
* 顶视图、正视图、侧视图
* 炉次缩略图切换
* 左侧炉膛卡片联动 3D
* 右侧物料选中联动 3D 高亮
* 爆炸图模式
* 分层 BOM / 施工清单
* 装炉动画播放、暂停、停止

### 9.3 显示状态

系统应正确处理：

* 无方案时显示空状态。
* 生成方案后显示 3D。
* 清空物料后隐藏方案相关按钮。
* 导入 JSON 后显示方案按钮。
* 方案库加载后恢复 3D 与按钮状态。
* 无装炉结果时不得显示 PDF / 动画 / 保存方案按钮。

---

## 10. 方案库需求

### 10.1 方案库定位

方案库不是简单的 JSON 文件管理，而是历史装炉经验的入口。

当前方案库采用浏览器 localStorage 保存，未来可迁移到数据库或飞书多维表格。

### 10.2 当前能力

方案库当前支持：

* 保存当前装炉方案
* 显示方案列表
* 显示方案详情
* 加载方案到工作台
* 导出方案 JSON
* 删除方案
* 保存方案摘要

### 10.3 方案摘要

方案卡片应显示：

* 方案名称
* 创建日期
* 策略
* 炉次数量
* 工件数量
* 总重量
* 首炉名称
* 首炉尺寸

### 10.4 历史方案恢复要求

从方案库加载时必须恢复：

* 炉膛 / 工装配置
* 工装参数
* 物料批次
* 客户信息
* 产品名称
* 物料编码
* 材质
* 工艺
* 硬度
* 日期
* 备注
* CAD 图
* 装炉结果
* 搁板
* 当前规则配置
* 分析面板

---

## 11. JSON 导入导出需求

### 11.1 导出

用户生成方案后，可导出标准 JSON。

JSON 应包含：

* meta
* process / rules
* materials
* tooling
* loadingPlan
* predictions
* audit

### 11.2 导入

系统支持导入标准 JSON：

```text
schemaVersion: heat-treatment-digital-twin-v1
recordType: furnace-loading-record
```

旧格式历史方案暂不直接混入新版方案库，避免格式混乱。

### 11.3 数据完整性要求

导入导出后，以下数据不得丢失：

* 物料尺寸
* 数量
* 重量
* 客户
* 产品显示名
* 物料编码
* 材质
* 工艺
* 硬度
* 下单日期
* 交付日期
* 备注
* CAD 图
* 搁板
* 装炉坐标
* 工装参数
* 装炉规则

---

## 12. PDF 作业指导书需求

PDF 导出用于给车间现场执行装炉。

当前目标不是生成复杂仿真报告，而是生成清晰的工业作业指导书。

PDF 应包含：

1. 方案总览。
2. 炉膛 / 工装信息。
3. 物料清单。
4. 装炉多视图。
5. 分层图。
6. 搁板说明。
7. 爆炸图或装配路径图。
8. 物料图例。
9. 统计信息。
10. 签字确认区。

PDF 风格应接近：

* IKEA / LEGO 式步骤说明
* 工程图纸式布局
* 适合工人现场快速理解
* 尽量减少复杂术语

---

## 13. 预测与评分需求

当前预测引擎定位为“经验型近似评分”，不是实时物理仿真。

系统可输出：

* 空间利用率
* 重量利用率
* 工件数量
* 未装入数量
* 材质聚集率
* 工艺聚集率
* 热场均衡评分
* 表面均匀性评分
* 综合评分
* 简化能耗预估
* 简化质量风险提示

后续可接入历史数据校准模型。

---

## 14. UI / UX 设计原则

当前 UI 应遵循：

1. 工业软件清晰优先。
2. 3D 视图居中，不被复杂面板遮挡。
3. 左侧管理炉膛 / 工装。
4. 右侧管理物料、分析、规则。
5. 中部显示 3D 与方案统计。
6. 重要按钮状态明确。
7. 避免过多弹窗。
8. 支持轻量 Apple / Tesla / Linear 风格。
9. 不追求炫技，应优先让工厂用户看懂。

---

## 15. 当前模块结构

当前主要模块包括：

| 文件                    | 责任                   |
| --------------------- | -------------------- |
| app.js                | 应用启动、主流程协调、事件绑定      |
| state.js              | 全局状态管理               |
| ui.js                 | UI 卡片、面板、导入预览、筛选条    |
| three-scene.js        | Three.js 渲染、动画、3D 交互 |
| furnace-engine.js     | 装炉算法入口与多策略执行         |
| PackingRuleEngine.js  | 工艺 / 材质分组规则          |
| strategies.js         | 装炉策略评分权重             |
| geometry-utils.js     | 姿态优化与几何辅助            |
| basket-model.js       | 工装 / 料框模型            |
| plan-record.js        | 标准装炉记录构建与解析          |
| plan-library.js       | 本地方案库                |
| plan-analysis.js      | 方案分析与评分              |
| prediction-engine.js  | 简化预测                 |
| pdf-export.js         | PDF 选择与基础导出          |
| pdf-six-page.js       | 六页式 PDF 生成           |
| screenshot-capture.js | 截图捕获                 |
| main.css              | 主样式                  |
| pdf-template.css      | PDF 模板样式             |

---

## 16. 当前已完成能力

### 16.1 数据闭环

已完成：

* 物料卡片读取。
* 算法输入透传业务字段。
* packedItems 保存业务字段。
* JSON 保存业务字段。
* 方案库保存业务字段。
* JSON 导入恢复业务字段。
* 方案库加载恢复业务字段。
* CAD 图在 materials 层保存和恢复。

### 16.2 方案库闭环

已完成：

* 保存方案。
* 查看方案列表。
* 查看方案详情。
* 加载方案。
* 删除方案。
* 导出方案 JSON。
* 从方案库恢复规则与搁板。

### 16.3 UI 状态闭环

已完成：

* 生成方案后显示 PDF / 动画 / 保存方案按钮。
* 无结果时隐藏方案按钮。
* 清空物料 / 清空料框时隐藏方案按钮。
* JSON 导入后显示方案按钮。
* 方案库加载后显示方案按钮。
* 爆炸图 / BOM 按钮独立控制。

### 16.4 代码清理

已完成：

* 方案库从 app.js 拆分到 plan-library.js。
* 按钮显示状态封装为 showPlanActionButtons / hidePlanActionButtons。
* 保留 app.js 作为主流程协调器。

---

## 17. 当前待办优先级

### P0：代码结构继续清理

1. 拆出 workbench-record.js
   包含：

   * restoreWorkbenchInputsFromRecord
   * loadDigitalTwinRecordToWorkbench
   * applyDigitalTwinRecordToWorkbench

2. 拆出 tooling-modal.js
   包含：

   * openToolingAddModal
   * renderToolingTypeCards
   * selectAddToolingType
   * confirmAddTooling
   * renderEmptyToolingOnly

3. 保持每次拆分后完整回归测试。

### P1：方案复用与模板

1. 保存为模板。
2. 从历史方案复制新方案。
3. 根据客户 / 产品 / 工艺匹配历史方案。
4. 给相似订单推荐历史装炉方式。

### P2：人工编辑

1. 允许用户微调物料位置。
2. 支持重新保存人工调整后的方案。
3. 记录 AI 原始方案与人工修改差异。
4. 为后续 AI 学习提供数据。

### P3：飞书订单接入

1. 从飞书多维表格读取订单。
2. 将飞书订单转换为物料批次。
3. 装炉完成后回写方案 ID。
4. 支持订单状态流转。
5. 支持司机取货 / 外部叫车信息采集。

### P4：历史质量与能耗反馈

1. 记录实际处理结果。
2. 记录合格率、硬度、变形、返工等。
3. 记录实际能耗。
4. 对比预测值与实际值。
5. 逐步形成经验模型。

---

## 18. 未来路线

### 阶段 1：当前工作台稳定

目标：

* 单机可演示。
* 能导入数据。
* 能生成方案。
* 能保存方案。
* 能恢复方案。
* 能导出 PDF。

### 阶段 2：历史方案模板化

目标：

* 同类订单快速复用历史方案。
* 老师傅经验可沉淀。
* 用户可从历史方案创建新方案。

### 阶段 3：人工微调与经验学习

目标：

* 用户可修改 AI 方案。
* 系统记录人工修改。
* 后续 AI 可学习用户偏好和工厂经验。

### 阶段 4：飞书订单管理接入

目标：

* 用飞书作为轻量数据后台。
* 解决订单收集、司机下单、小单流失、取货方式判断等业务问题。
* 将订单信息与装炉方案关联。

### 阶段 5：工厂级系统化

目标：

* 对接数据库。
* 多用户权限。
* 历史质量回流。
* 能耗分析。
* MES / ERP / 设备数据逐步接入。

---

## 19. 非目标说明

为了避免产品方向失焦，当前阶段明确不做：

1. 不做完整 APS 排产系统。
2. 不做真实实时 SCADA 监控。
3. 不做复杂 CFD 热仿真。
4. 不做完整 MES。
5. 不做复杂权限系统。
6. 不做移动端优先。
7. 不做 CAD 精确识别。
8. 不做自动报价。
9. 不做真实工艺参数闭环控制。

当前产品应先把“装炉前规划”做准、做清楚、做可复用。

---

## 20. 成功标准

当前阶段成功标准：

1. 用户能在 5 分钟内创建一个炉膛和一批物料。
2. 用户能一键生成多个候选方案。
3. 用户能在 3D 中看懂装炉结果。
4. 用户能看到未装入物料和容量不足原因。
5. 用户能保存方案到方案库。
6. 用户能从方案库完整恢复方案。
7. 用户能导出 JSON 并再次导入。
8. 用户能导出 PDF 给工人参考。
9. 物料业务字段不丢失。
10. 搁板、坐标、工装参数不丢失。
11. app.js 持续变轻，代码结构可维护。

---

## 21. 一句话产品定义

工业热处理预装炉智能体，是一个帮助热处理工厂在进炉前完成“物料整理、装炉规划、3D 验证、方案保存、作业指导”的轻量数字孪生工作台。

它当前不是完整 MES，也不是实时仿真平台，而是工厂装炉经验数字化的第一步。
